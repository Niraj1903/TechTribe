// const auth = "XYZ";

// const login = (req, res, next) => {
//   const token = auth === "XYZ";
//   if (!token) {
//     res.status(401).send("Error 401");
//   } else {
//     next();
//   }
// };

// module.exports = { login };
